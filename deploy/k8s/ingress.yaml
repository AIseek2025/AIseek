apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aiseek
spec:
  rules:
    - host: aiseek.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aiseek-backend
                port:
                  number: 80
